.DS_Store

# Environment and secrets
.env
.env.*
!.env.example
server/.env
server/.env.*

# Test seed credentials (real Telegram session, never commit)

# Python
__pycache__/
.pycache/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
server/data/
*.session
*.session-journal
# Vestigial nested backend tree from an early scaffold; never used at runtime.
server/server/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Builds and simulator output
artifacts/
*.ehpk
*.png
# Old blank-glasses PNG goldens are not tracked (replaced by JSON goldens
# in the same directory). New `.glasses.json` golden files are tracked
# because they contain real captured render models that the harness
# diffs against at runtime — keeping them in git means a stale
# `npm run test:simulator` will surface content regressions.
!web/test/simulator-goldens/*.glasses.json
SECURITY_PRIVACY_FUTURE.md
dist/
build/
.evenhub/
web/test/seed-credentials.local.json
web/test/seed-credentials.json
.vite/
coverage/

# Others
.graphiq/
.a5c/
