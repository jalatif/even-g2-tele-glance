 Another agent wrote this code and is making wrong conclusions on app not working due to simulator bug. At highlevel, glasses app is supposed to show chat group or
  user thread on left side and messages on right side. By default cursor or selection is on left side. Selecting (via single click/press) on left side chat group with
  which has topics should switch left view to list of topics. up/down on chat groups/topics should move selection cursor to up and down. If chat with no topics is
  selected or when topic is selected (with single click) then focus should shift on right side messages. Once focus is on right side messages view, up/down should scroll the messages. there should be no ghost text while scrolling. Click on messages view should record, single click to stop recording, double click cancel record. double click on right message view should shift cursor back to left side and then scroll should be on topics/chats. double click on topics moves state back to list of chats. double click on chats screen toggles on/off screen. Messages scrolling and opening chats should be very fast, user should not notice any delays so we should do as much prefetching as possible.
